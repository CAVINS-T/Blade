import 'package:flutter/material.dart';

class InferenceTest extends StatelessWidget {
  const InferenceTest({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('inference_test.dart placeholder')));
}