import 'package:flutter/material.dart';

class Fitfinder extends StatelessWidget {
  const Fitfinder({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('fitfinder.dart placeholder')));
}