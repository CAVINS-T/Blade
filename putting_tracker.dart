import 'package:flutter/material.dart';

class PuttingTracker extends StatelessWidget {
  const PuttingTracker({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('putting_tracker.dart placeholder')));
}