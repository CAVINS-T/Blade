import 'package:flutter/material.dart';

class SwingResults extends StatelessWidget {
  const SwingResults({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('swing_results.dart placeholder')));
}