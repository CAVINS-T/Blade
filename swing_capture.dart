import 'package:flutter/material.dart';

class SwingCapture extends StatelessWidget {
  const SwingCapture({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('swing_capture.dart placeholder')));
}