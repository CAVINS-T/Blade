import 'package:flutter/material.dart';

class PuttingStatsDashboard extends StatelessWidget {
  const PuttingStatsDashboard({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('putting_stats_dashboard.dart placeholder')));
}