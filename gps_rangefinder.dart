import 'package:flutter/material.dart';

class GpsRangefinder extends StatelessWidget {
  const GpsRangefinder({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(body: Center(child: Text('gps_rangefinder.dart placeholder')));
}